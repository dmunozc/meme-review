<li>
	<%= link_to user.name, user%>
	<% if current_user.admin? && !current_user?(user)%>
		<%= link_to t('.delete'),user,method: :delete, data: { confirm: t('.confirmation',name: user.name ) }%>
		
	<%end%>
</li>